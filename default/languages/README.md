This directory contains the translated messages.

The file `list.json` lists all known languages.
All other files, in the format `<langcode>.json`, contain the translated messages for a given language.
