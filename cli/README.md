Here are the command-line scripts intended for administrator access.

All scripts here will show their documentation if you pass the parameter `--help` (or `-h`). For example
    cli/quotations.php --help
